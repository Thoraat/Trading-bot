services:
  - type: web
    name: backend-service
    env: docker
    rootDir: backend/demo
    dockerfilePath: backend/demo/Dockerfile
    plan: free
    envVars:
      - key: PORT
        value: 8080