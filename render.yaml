services:
  - type: web
    name: final-trade-backend
    env: docker
    rootDir: backend/demo/Dockerfile
    plan: free         # or starter / standard
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8080
      # - key: API_KEY
      #   sync: false   # Set in Render dashboard (not in Git)